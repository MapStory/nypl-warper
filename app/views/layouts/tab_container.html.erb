<%# If these are all xhr requests, what is the head doing in here? %>
<head>
<%= yield :head %>
<%= csrf_meta_tags %>
<%= javascript_tag @onload -%>
</head> 
<script type="text/javascript">
  var xhrFlag = "<%=@xhr_flag%>";
</script> 

<%= flash_messages %>

<%= yield %> 
<%= render :partial => 'shared/analyticsbottom',
  :locals => {
  :map => @map,
  :current_tab => @current_tab
} %>	
