# System library as opposed to gem, so we have to load it.

require 'mapscript'

# For some reason the previous code had 
# include Mapscript
# but it always references it with the namespace, so unsure why
# including...
